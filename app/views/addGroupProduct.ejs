<%- include('header') %>

<form method="post">
    <div class="card mt-3">
        <div class="card-body">
            <div class="card-title h3">เพิ่มรายการ ประเภทสินค้า</div>
            <div class="mt-3">
            <label>ชื่อ</label>
            <input type="text" name="name" class="form-control" value="<%= groupProduct.name %>" />
        </div>
        <div class="mt-3">
            <button type="submit" class="btn btn-info">
                Save
            </button>
        </div>
        </div>
    </div>
</form>

<%- include('footer') %>